name: "Approve Dependabot PR's" 
on:
  pull_request:

permissions:
  contents: write
  pull-requests: write
  actions: write
  
jobs:
  dependabot:
    uses: devops-actions/.github/.github/workflows/approve-dependabot-pr.yml@ed6b9ab5002c46eb78ceba4abdfc4ae46618e107 # main
    secrets:
      GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
